# Strumenti per lint e test (CI GitHub e sviluppo locale).
# NON servono sul builder Jenkins, che esegue solo ansible-playbook.
#
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install -r requirements-dev.txt
#   molecule test          # test completi (finge un device)
#   ansible-lint && yamllint .
ansible-core>=2.16
ansible-lint>=24.0
yamllint>=1.35
molecule>=25.0
shellcheck-py>=0.10
